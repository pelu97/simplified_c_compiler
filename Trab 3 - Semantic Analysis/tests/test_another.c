float maior_numero(float list lista, int num) {
    int i;
    int numero;
    for(i = 0; i < num; i = i + 1) {
        if(numero > ?lista) {
            numero = ?lista;
        }
        lista1 = %lista;
    }
    return numero;
}

int main() {
    writeln("Digite um numero inteiro positivo: ");
    num = 0;
    int num;
    float x = 1.1;
    int y;
    y = x + num;
    read(num);
    if(num > 0) {
        int list x;
        write(main);
        main(3);
    }
    x = 1;
    return numero;
}

int main() {
    int num;
    read(num);
    return numero;
}
